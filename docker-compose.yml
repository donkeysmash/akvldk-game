version: '3'
services:
  server:
    build: .
    ports:
      - 3000:3000
      - 9222:9222
    command: npm run debug
    volumes:
      - ./dist_server:/app/dist_server
    environment:
      - PORT:3000